# Golden corpus — a parse error (domain=parse).
#
# An assignment with no right-hand side. The error-tolerant parser records the
# rich diagnostic and renders it through the unified renderer.
pub struct ParseError {
  pub fn oops() -> i64 {
    value =
  }
}

fn main(_args :: [String]) -> u8 {
  ParseError.oops()
}
