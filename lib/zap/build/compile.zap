@doc = """
  Compile the current manifest artifact.

  The step uses the same source discovery, dependency resolution,
  memory manager selection, cache, and ZIR backend path as the default
  manifest build.
  """

pub struct Zap.Build.Compile {
}
